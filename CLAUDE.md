# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This repository currently contains a single static HTML file, `archivo.html`: a simple counter page with "Sumar" (increment) and "Restar" (decrement) buttons, styled with a dark background. There is no build system, package manager, dependencies, or test suite.

## Running

Open `archivo.html` directly in a web browser — no build or server step is required.
